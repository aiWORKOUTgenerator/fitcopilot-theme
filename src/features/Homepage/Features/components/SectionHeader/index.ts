/**
 * SectionHeader component exports
 * Provides reusable section header functionality for the Features section
 */

export { SectionHeader } from './SectionHeader';
export type {
    SectionHeaderA11y, SectionHeaderAnimation, SectionHeaderConfig, SectionHeaderProps
} from './types';

// Re-export types from parent for convenience
export type { VariantKey } from '../../types';
